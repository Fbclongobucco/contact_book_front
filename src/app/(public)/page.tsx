import { Login } from "@/components/login";

export default function LoginPage() {
  return (
    <main className="w-full h-screen bg-gradient-to-b from-cyan-200 to-zinc-300 flex justify-center items-center">
      <Login/>
    </main>
  );
}
